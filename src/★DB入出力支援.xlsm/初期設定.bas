Attribute VB_Name = "初期設定"
' ---------------------------------------------------------------------------------------------------------------------
' 定数
' ---------------------------------------------------------------------------------------------------------------------

Const cnstインデント = "   "

' ---------------------------------------------------------------------------------------------------------------------
' 変数
' ---------------------------------------------------------------------------------------------------------------------

Public globalWb前回実行結果 As Workbook

' *********************************************************************************************************************
' * 機能　：表示非表示の切替
' *********************************************************************************************************************
'
Private obj設定値シート As cls設定値シート

Sub グループ表示非表示の切り替え()

    Dim obj試験データシート As cls試験データシート
    Set obj試験データシート = New cls試験データシート
    
    obj試験データシート.表示非表示の切替
    
End Sub

' *********************************************************************************************************************
' * 機能　：フォームの表示
' *********************************************************************************************************************
'
Sub createSELECT文()

    frmSQL生成.Show vbModeless
    
    Set obj設定値シート = New cls設定値シート
    obj設定値シート.ロード

    Call frmSQL生成.設定値ロード(obj設定値シート)
    
    Excel.Application.CutCopyMode = False
    
End Sub

' *********************************************************************************************************************
' * 機能　：ブックオープン時の処理
' *********************************************************************************************************************
'
Sub Auto_Open()

    Set obj設定値シート = New cls設定値シート
    obj設定値シート.ロード

    Application.OnKey "%{F9}", "createSELECT文"

End Sub

' *********************************************************************************************************************
' * 機能　：テンプレート作成
' *********************************************************************************************************************
'
Public Sub テンプレート作成(Optional argサイレントモード As Boolean = False)

    'エラートラップ
    On Error GoTo ErrorCatch

    Dim objアプリケーション制御 As New clsアプリケーション制御
    objアプリケーション制御.アプリケーション制御切替 (False)

    ' -----------------------------------------------------------------------------------------------------------------
    ' 本処理開始
    ' -----------------------------------------------------------------------------------------------------------------

    Dim objテンプレート作成 As clsテンプレート作成
    Set objテンプレート作成 = New clsテンプレート作成

    objテンプレート作成.テンプレート作成

    ' -----------------------------------------------------------------------------------------------------------------
    ' 本処理終了
    ' -----------------------------------------------------------------------------------------------------------------

   ' 正常終了
   GoTo Finally
   
ErrorCatch:

    subエラー表示 (argサイレントモード)

Finally:

    ' 実行前の状態に戻す
    objアプリケーション制御.アプリケーション制御切替 (True)

End Sub

